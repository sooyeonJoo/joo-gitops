apiVersion: v1
kind: Service
metadata:
  name: issue-service
  namespace: eris-be
spec:
  selector:
    app: issue-service
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
